# MAIN

## Sequência introdutória

### 1.
git commit; git commit

### 2.
git branch bugFix; git checkout bug 

### 3.
git checkout -b bugFix; git commit; git checkout main; git commit; git merge bugFix

### 4.
git checkout -b bugFix; git commit; git checkout main; git commit; git checkout bugFix; git rebase main

## Acelerando

### 1.
git checkout c4

### 2.
git checkout bugFix; git checkout HEAD^1

### 3.
git checkout HEAD~1; git branch -f bugFix HEAD^; git branch -f main c6

### 4. 
git reset HEAD~1; git checkout pushed;  git revert HEAD

## Movendo trabalho por aí

### 1. 
git cherry-pick c3 c4 c7

### 2.
git rebase -i HEAD~4

## Sortidos

### 1.
git checkout main; git cherry-pick c4

### 2.
git rebase -i HEAD~2; git commit --amend; git rebase -i HEAD~2; git checkout main; git merge caption

### 3.
git checkout newImage; git commit --amend; git checkout main; git cherry-pick newImage; git cherry-pick caption

### 4.
git tag v0 c1; git tag v1 c2; git checkout v1

### 5.
git commit

## Temas avançados

### 1. 
git checkout bugFix; git rebase main; git checkout side; git rebase bugFix; git checkout another; git rebase side; git checkout main; git rebase another

### 2. 
git branch bugWork main~^2~;

### 3.
git checkout one; git cherry-pick c4 c3 c2; git checkout two; git cherry-pick c5 c4 c3 c2; git branch -f three c2;

## Remote

## Push & Pull

### 1. 
git clone

### 2. 
git commit; git checkout o/main; git commit;

## 3.
git fetch

## 4. 
git pull

## 5.
git clone; git fakeTeamwork main 2; git commit; git pull;

## 6.
git commit; git commit; git push;

### 7.
git clone; git fakeTeamwork; git commit; git pull --rebase; git push;

### 8.
git checkout -b feature; git push origin feature; git checkout feature; git branch -f main c1

## Push Main!

### 1.
git fetch; git rebase o/main side1; git rebase side1 side2; git rebase side2 side3; git rebase side3 main; git push;

### 2.
git checkout main; git pull; git merge side1; git merge side2; git merge side3; git push;

### 3.
git checkout -b side o/main; git commit; git pull --rebase; git push;

### 4.
git push origin main; git push origin foo;

### 5.
git push origin main~1:foo; git push origin foo:main;

### 6.
git fetch origin c6:main; git fetch origin c3:foo; git checkout foo; git merge main

### 7.
git push origin :foo; git fetch origin :bar;

### 8.
git fetch origin c3:foo; git fetch origin c2:side; git merge foo; git merge side;